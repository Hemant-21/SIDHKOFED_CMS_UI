/**
 * `/dashboard/website-metrics` — Website Metrics list (Stage 5).
 */
import { WebsiteMetricsListPage } from '@/features/dashboard-data';

export default function DashboardWebsiteMetricsRoute() {
  return <WebsiteMetricsListPage />;
}
